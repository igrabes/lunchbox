<h1>Listing users</h1>

<table>
  <tr>
    <th>testing</th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
		<td><%= link_to user.email, user %></td>  
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete, :remote => true, :class => 'delete_post' %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
